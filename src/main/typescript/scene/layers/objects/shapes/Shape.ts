import {AbstractLayerObject} from "scene/layers/objects/AbstractLayerObject";

/**
 * Shape.
 *
 * Special Layer Object type that is used for interactions with other Layer Objects.
 */
export default class Shape extends AbstractLayerObject {
}